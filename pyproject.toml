[project]
name = "mev-dev-tools"
version = "1.0.0"
description = "Development-only Python dependencies for mev"
requires-python = ">=3.12,<3.13"

[dependency-groups]
dev = [
    "ansible-lint>=25.0.0,<27.0.0",
    "ansible>=11.0.0,<12.0.0",
]